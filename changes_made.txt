# --------------- Database.java ---------------

1. [Done] Add the area filter 
    - "HDB flats whose area is not smaller than 80 square meters (≥ 80m2)" -> Darren's code only has town filter
    - Need to change the query methods:
        1. public ArrayList<Integer> queryDBIndex()
        2. public ArrayList<Integer> queryDB()
        3. public ArrayList<Integer> queryCompressedDB()

2. [Done] Modify the town mapper array
    - "List of towns corresponding to the third last digit in matriculation number" -> ours is different from Darren's
    - Need to change
        - String[] town_mapper

3. [Done] Modify the month mapper array
    - "resale HDB flats whose area is not smaller than 80 square meters (≥ 80m2) within a specific town over two consecutive months in a given year." -> Ours is 2 and Darrens is 3
    - Need to change:
        - String[][] monthMapperStr 
        - int[][] monthMapper

4. [Done] Add function for "Minimum Price per Square Meter"
    - add new function public void calculateMinPricePerSqm()

5. [Done] Change the outputs 
    - Darren's requirement is "Minimum Area", "Average Area", "Standard Deviation of Area", "Minimum Price", "Average Price", "Standard Deviation of Price"
    - Ours is "Minimum Price", "Average Price", "Standard Deviation of Price", "Minimum Price per Square Meter"
    - Need to change:
        - final String[] typeText
        - public void calculateDefault()


# --------------- TO AVOID PLAGARISM ---------------
1. Restructure the Code Organization
    - Create a separate Statistics class: Move all the statistics calculation logic into a dedicated class
    - Implement a QueryFactory pattern: Create a factory class that produces different query strategies
    - Use a configuration file: Store query parameters in a properties or JSON file instead of hardcoding them

2. Improve the Algorithm Implementations
    - Change the sorting algorithm: Replace the heapSort with a different algorithm like quicksort or mergesort
    - Modify the index structure: Implement a different indexing structure like a B-Tree or hash-based index
    - Implement a different compression technique: Try run-length encoding or dictionary-based compression

3. Add New Features
    - Implement a caching layer: Add a cache to store recent query results
    - Create a more sophisticated query planner: Build a cost-based optimizer that chooses the best execution strategy
    - Add parallel query execution: Split queries into subtasks that can run concurrently
    - Implement different partition strategies: Experiment with range or hash partitioning for the data